---
id: "[1.1.1]"
title: CLI Flags Reference
subtitle: Full control from the command line
tags: [cli, flags, reference]
difficulty: Intermediate
status: Published
completeness: 100
depends_on: ["[1.1.0]"]
---

# CLI Flags Reference

The `pretty-converter build` command accepts these flags:

## Config & Source

```bash
pretty-converter build \
  --config custom.yml \
  --source ./docs \
  --out mybook.pdf
```

## Metadata

```bash
pretty-converter build \
  --title "{{product}} Guide" \
  --subtitle "A complete reference" \
  --author "{{company}}"
```

## Theme & Style

```bash
pretty-converter build \
  --theme minimal \
  --css ./custom.css \
  --template ./custom-template.html
```

## Render Options

```bash
pretty-converter build \
  --timeout 30s \
  --verbose
```

<Warning title="Precedence">
CLI flags **always override** config file values.
Config file values override defaults.
</Warning>
